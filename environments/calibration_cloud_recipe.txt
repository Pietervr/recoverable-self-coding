# Recorded direct dependencies; not a full historical transitive lock.
# Python 3.12
# Cloud job recipe; calibration rows have no runtime field.
jax==0.11.1
numpy==2.4.6
scipy==1.18.0
pandas==3.0.5
joblib==1.5.3
