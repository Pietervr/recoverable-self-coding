# Second-opinion brief for Codex — T1 pre-registration v1.2 and the simulation run (2026-09-11)

**Ask.** You reviewed `PREREGISTRATION_T1_model.md` twice at v1.1 (the amendment table records both
rounds). Since then the analysis code was written and the pre-registration moved to draft v1.2 with
changes made by Claude (session Entropy) without review. The §7.5 / §10 simulations are **running now**
in the cloud (50 SageMaker jobs, about 27 h, about USD 2,900). A flaw found in the next few hours costs
a relaunch of the affected stage; one found after the run costs the run again. Read as a reviewer:
are the simulation goals and the direction sound, and is anything blocking? Answer the numbered
questions, then give one verdict line.

**Where things are** (all under `workspace_demo/t1_access/` of this repository):
- `PREREGISTRATION_T1_model.md` — draft v1.2. The v1.1 → v1.2 diff is
  `git diff e46bbcf HEAD -- workspace_demo/t1_access/PREREGISTRATION_T1_model.md`.
- `models.py` (§7 members, §7.4 fitting and quadrature), `analyze.py` (§8), `simulate.py` (§7.5, §10:
  generators, grid, gain calibration, power alternatives), `verify.py` (what was checked),
  `README.md` (the decisions the code made that the text had left open), `t1_job.py` / `launch_t1.py`
  (the cloud jobs), `spotcheck.py` (what is read while the run goes).
- The run: 12 graded nulls and 12 mixture alternatives at 1,000 replicates, the 48 recovery grid points
  at 200, a five-layer check at 200; D = 4 only; one synthetic layer per dataset for the main stages.

**What v1.2 changed (each is a candidate for your objection):**
1. §7.4 fitting: L-BFGS-B on exact JAX gradients replaces Nelder–Mead; the start generator is defined
   (training-fold moments, start 0 unjittered, 7 jittered N(0, 0.25²)); *converged* = L-BFGS-B success.
2. §7.4 quadrature: the joint concept likelihood's random-effect integral is a per-concept trapezoid rule
   on [mode ± 6 Laplace SD] (mode by a 9-point grid start and 4 backtracking Newton steps; SD floored at
   tau). Prior-centred Gauss–Hermite failed at n_c = 168 (80 nodes miss by 0.06–0.3 nat); mode-centred
   Gauss–Hermite failed at tau = 2 for concepts whose shifted threshold leaves the level range (truncated
   posteriors, 1–5 nat off). The trapezoid rule is within 6e-7 nat of dense quadrature across the grid
   except those truncated M2H posteriors at tau = 2, where 96 points still leave 0.02 nat per concept
   (1e-4 per trial). Point-count rule 48 → 96 → 192 on CAL/PILOT.
3. §14: inner selection uses 4 starts (the clause invoked for the simulation budget; refits keep 8;
   CONF will use the same). On the benchmark the selections and Delta are identical to 8 starts.
4. §7.3: the M3V floor is implemented as sigma = floor + e^s.
5. §8.2: foil pairs cannot be kept together under family strata (pairs cross families); the primary R1
   bootstrap resamples concepts within family strata; the paired statistics of §6.4 / §8.5(b) resample
   foil pairs as units, unstratified.
6. §7.5 / §10 generator base (declared in `simulate.py`): graded mu(0) = −1, mu(8) = +1, x0 = 3,
   kappa = 1.5, sigma = |0.15 mu + 0.75|; mixtures mu_low = −1, sigma = 0.6 (M3V: 0.6 / 0.9),
   A(k) = lg(1.5 (k − 3)), mu_high(k) = mu_low + sep·sigma + 1.0·lg_h(k); M2K alpha0 = alpha, alpha1 = 0;
   the inherited M3 as generator gets the offset through `step` (its high mean is absolute).
7. §10 gain: "per-trial gain" is made operational as the expected out-of-sample joint log-score advantage
   per trial of the generator over the best graded member (M2B, M2H, M2S, M2K) fitted at large sample
   (256 concepts, best by training joint log-likelihood), reached by bisection on a `scale` that
   multiplies both high-state offsets (`simulate.calibrate_gain`); M3H at tau = 0.5, M3L at pi0 = 0.05.
8. §10 coverage: the 95 % CI's coverage is scored against the replicate mean of the point estimate under
   each generator (an estimand proxy at the design size), not against a large-sample quantity.
9. §10 layers: the main stages use one synthetic layer per dataset (the band mean of one layer); the
   five-layer check (M2B, M2K nulls; M3H, M3V alternatives; layers 25, 33, 41, 49, 57, AR(1) residual
   correlation rho = 0.9 across layers, the mixture state shared across a trial's layers, the concept
   effect shared) is meant to show the layer averaging changes nothing. PILOT's measured correlation
   is not available yet.
10. Counts: the pre-registered 1,000 per §10 setting and 200 per §7.5 point are what runs.

**Questions.**
- Q1. Do these simulations, as specified, answer §10's questions for the §8.3 decision rule
  (false-positive rate ≤ 0.064 under every graded null, coverage ≥ 0.90, power ≥ 0.8 at 0.01 nat under
  every mixture alternative at the chosen D)? Is any generator, grid, estimand or count choice one that
  makes the run's answer uninterpretable or not the pre-registered quantity?
- Q2. Is the gain operationalisation (item 7) the right reading of "per-trial gains 0.003 (the human
  scale), 0.01 and 0.03 nat", and is scaling both offsets the right way to move along the alternative?
- Q3. Is the coverage proxy (item 8) acceptable, or must the estimand be defined otherwise before the
  results are read?
- Q4. Is one layer per dataset plus the five-layer check adequate for a band-mean statistic, or does the
  calibration need the band from the start?
- Q5. Numerics (items 1–3): does anything bias the family comparison — the 0.02 nat residual at tau = 2,
  the 4 inner starts, the convergence definition, the trapezoid window?
- Q6. The foil-pair bootstrap decision (item 5).
- Q7. Separate what must be fixed **before the results are read** (blocking — say what to stop and rerun)
  from what can wait for the v2 freeze.

**Format.** Numbered findings, each with severity (blocking / should fix at v2 / note) and a pointer
(section or file:line). End with one verdict line: `VERDICT: on board` or `VERDICT: not on board —
<the blocking items>`. Read the files; do not edit anything and do not run the simulations. Keep it to
what matters.
