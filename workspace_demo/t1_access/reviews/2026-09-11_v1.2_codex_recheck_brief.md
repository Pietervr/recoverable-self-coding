# Re-check brief for Codex — the v1.2 blockers as fixed (2026-09-11, commit 52267ec)

**Ask.** Your review of v1.2 (`2026-09-11_v1.2_codex_brief.md`; verdict "not on board") stopped the
first cloud run after one hour. The blocking items are fixed at commit `52267ec` and the corrected
run has been relaunched under a new namespace (`results/t1_access/d4v12b/`, code snapshot
`code/t1_access/d4v12b/`). Please re-check the fixes against your findings 1–5, 7, 10 and 12, bounded
to what could still invalidate the run; the v2 items (6, 8, 9, 11) are noted for the freeze. Read-only.

**What changed, by finding:**
1. Quadrature (`models.py` `_agh_centre`, `_concept_loglik`; `audit_quadrature.py`): the Laplace SD is
   stated as capped at tau; the rule is two-scale — a 96-point trapezoid on [mode ± 6 s_hat] plus two
   32-point trapezoid rules with exact endpoints on the rest of [−6 tau, 6 tau], so flat tails and a
   second mode are integrated; the mode search backtracks over eight geometric fractions (three left
   M2S at omega = 2 stuck at its grid start at fitted parameters). The audit compares with dense
   quadrature (80,001 points over ±10 tau) at generating parameters, at fitted parameters on held-out
   and training concepts, and under six cross-fits: max 3.1e-4 nat in every generating/fitted case;
   3.6e-3 in one cross-fit (M3H on M2S omega = 2: a staircase likelihood in u from a very large fitted
   slope, error falling linearly with P), bound 2e-5 nat per trial on the estimand; tolerances 1e-3
   and 5e-3 stated in §7.4. The ladder rule is 64 → 96 → 128.
2. M3V truth (`simulate.expected_gain`): the truth term is scored on a Trials object with floor 0; the
   old scoring was off by 0.0066 nat per trial. Gains are recalibrated by every power stage (item 7).
3. Single-layer scope (§10 rewritten): the single-layer run is stated as the per-layer calibration and
   family recovery; the band validation is its own post-PILOT stage; the five-layer stage is a
   computational pilot with every graded member (M2B; M2H 0.5; M2S 0.5; M2K 1) and every mixture
   member at 0.01 nat; `_ar1` correlates per physical layer (rho ** gap).
4. Inner failures (`analyze.layer_pipeline`): a non-finite inner fit or score gives −inf and cannot be
   selected; every member of a family unscorable → `failed` with a reason; `inner_convergence` and
   `inner_nonfinite` recorded per row.
5. Aggregation (`spotcheck.py`): exact raw-shard filename grammar, schema check, uniqueness of
   (generator, grid, rep, D, n_layers), single code hash per stage.
7. Gain gates (`simulate.calibrate_gain`, `calibrate_all_gains`, `power_points`): all twelve
   (member, target) pairs required, tolerance 5 %, bracket and limit failures explicit and refused, an
   independent achieved-gain estimate with concept-cluster SE at a fresh seed, D passed and cached
   by D (`gain_calibration_D4.json`); "0.003" is called a numerical reference scale in §10.
10. Versioning (`simulate.config_hash`, `run_points`, `launch_t1.upload_code`, `t1_job.gain_file`):
   a 12-hex hash over the three code files and the run settings in every row and in the gain file;
   resume refuses another hash; one immutable code snapshot per run namespace; per-concept Delta and
   per-member held-out log q per concept, and the selected members per layer and fold, saved in every
   row.
12. Recovery reuse: the recovery stage fits only the mixture generators; the graded grid points come
   from the calibration stage's first 200 replicates at analysis.

**Format.** Per finding: `fixed` / `not fixed — <why>`; then any NEW blocking item with a pointer; end
with `VERDICT: on board` or `VERDICT: not on board — <items>`. Reply with
`xs say claude:Entropy "<text>"`.
